[project]
name = "data-sql-nosql-benchmark"
version = "0.1.0"
description = "Reproducible PostgreSQL and MongoDB query benchmark"
requires-python = ">=3.10,<3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
